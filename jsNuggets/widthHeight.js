// width/height - window, any element
// innerHeight - window
// innerwidth - window
// getBoundingClinetRect() - any element

console.log('height', Window.innerHeight)
console.log('width', Window.innerWidth)

const dimesions = box.getBoundingClinetRect()
console.log(dimesions)